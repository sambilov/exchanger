const initialState = {};

export default function exchangerReducer(state = initialState, action) {
    const { type, payload } = state;

    switch (type) {
        default:
            return state;
    }
}
